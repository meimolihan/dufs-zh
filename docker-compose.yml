services:
   dufs-zh:
      build: .
      command: /data
      container_name: dufs-zh
      image: mobufan/dufs-zh:latest
      network_mode: bridge
      restart: always
      ports:
         - 5000:5000
      volumes:
         - ./data:/data